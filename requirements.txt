# =============================================================================
# ComfyUI-3D-Pack: Root requirements (base packages only)
# =============================================================================
# This file installs CUDA-independent packages. It is consumed by ComfyUI-Manager
# and pip install. CUDA-specific packages (torch, spconv, etc.) are handled by
# install.py which auto-detects your CUDA version.
#
# For manual installation with CUDA version control, see requirements/ folder:
#   python requirements/install_requirements.py          # auto-detect
#   pip install -r requirements/cuda12x.txt              # CUDA 12.x
#   pip install -r requirements/cuda13x.txt              # CUDA 13.x (Linux)
#   pip install -r requirements/cuda13x_windows.txt      # CUDA 13.x (Windows)
# =============================================================================

# build tools
cmake
ninja
pccm

# web tools
PyGithub
requests

# computational libraries (scipy pinned >=1.15 for Python 3.14 wheel support)
numpy
einops
scipy>=1.15.0
kornia
opencv-python
pillow
nerfacc>=0.5.3
slangtorch

git+https://github.com/EasternJournalist/utils3d.git#egg=utils3d
git+https://github.com/ashawkey/kiuikit.git#egg=kiui

# ML models
diffusers>=0.26.1
transformers>=4.36.2
safetensors
open_clip_torch
mmgp

# training differentiable tensors
torchmetrics
pytorch_msssim
pytorch-lightning
peft

# image & video processing
rembg
onnxruntime
imageio
imageio-ffmpeg
matplotlib
realesrgan

# mesh processing, import & export
trimesh
fast-simplification
plyfile
pygltflib
xatlas
pymeshlab
# gpytoolbox installed separately with --no-build-isolation (see install.py)
PyMCubes
libigl
pyvista
pymeshfix
igraph
open3d

# configs & extra
torchtyping>=0.1.5
tqdm
jaxtyping
packaging
OmegaConf
pyhocon
iopath
easydict